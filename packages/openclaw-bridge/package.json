{
  "name": "@strongest/openclaw-bridge",
  "version": "0.0.1",
  "private": true,
  "description": "Adapter shim that plugs an OpenClaw runtime into 0G — Sealed Inference gateway, encrypted state sync to 0G Storage, royalty hook, iNFT registry.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./llm-gateway": "./src/llm-gateway.ts",
    "./state-sync": "./src/state-sync.ts",
    "./royalty-hook": "./src/royalty-hook.ts",
    "./inft-registry": "./src/inft-registry.ts",
    "./crypto": "./src/crypto.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo \"(lint not configured yet)\""
  },
  "dependencies": {
    "@0gfoundation/0g-ts-sdk": "1.2.1",
    "@0glabs/0g-serving-broker": "*",
    "ethers": "^6.13.4",
    "chokidar": "^4.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0",
    "vitest": "^2.1.8"
  }
}
